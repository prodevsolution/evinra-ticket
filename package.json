{
  "name": "evinra-ticket-system",
  "version": "1.0.0",
  "description": "Evinra Ticket System (admin) — uses the shared evinra-shared-db service",
  "main": "server.js",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "better-sqlite3": "^12.10.0",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
